2356. Number of Unique Subjects Taught by Each Teacher
Difficulty - Easy

# Write your MySQL query statement below
select t1.teacher_id, count(distinct t2.subject_id) as cnt
from Teacher t1
join Teacher t2
on (t1.subject_id, t1.dept_id) = (t2.subject_id, t2.dept_id)
group by teacher_id
